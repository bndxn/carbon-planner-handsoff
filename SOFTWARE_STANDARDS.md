# Software standards

General guidance
* Be the minimal code required to get to the solution
* After each change, review and see whether there is any redundant code that can be removed

We should enforce linting and formatting checks using ruff. Docstrings should use the Google standard. There should be unit tests in pytest achieving a 70% coverage.