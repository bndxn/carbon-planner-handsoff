# Architecture

I want whatever app is designed to be serverless and low-cost. It should use an agent to query multiple resources.

There should be a front end web app that accesses stored information. The back end should run periodically. 

